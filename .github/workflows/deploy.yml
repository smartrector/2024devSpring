name: github actions

on:
  push:
    branches:
      - main

jobs:
  My-Deply-Job:
    runs-on: ubuntu-latest

    steps:
      - name: hello world
        run: echo "hello world"

      - name: 여러명령어 작성하기
        run: |
          echo "good"
          echo "morning"

      - name: github actions
        run: |
          echo $GITHUB_SHA
          echo $GITHUB_REPOSITORY

      - name: github action secret
        run: |
          echo ${{ secrets.MY_NAME }}
          echo ${{ secrets.MY_HOOBY }}
